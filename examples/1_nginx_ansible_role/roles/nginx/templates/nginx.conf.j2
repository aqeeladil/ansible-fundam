# If you want to customize the Nginx configuration file (optional)

server {
    listen 80;
    server_name localhost;

    location / {
        root /var/www/html;
        index index.html index.htm;
    }
}
